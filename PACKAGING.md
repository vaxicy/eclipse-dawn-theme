# Packaging

Zip the theme body (`manifest.json` + `logo/`) with `manifest.json` at the archive root and upload it as a new Chrome Web Store item. Keep the release at `version: 1.0.0` for the first upload.
